# Razroo NPM Starter
Includes Typescript 

## Easy way to use this boilerplate ##

1. Git clone into repo name that you want 
2. rm -rf .git 
3. Run git init 
4. Push to repo of choice. 

## To build ##
`npm run build`
`npm publish`
